{
  "name": "archive-kangsain-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "serve": "npm run build && firebase --config ../firebase.json emulators:start --only functions,firestore",
    "deploy": "npm run build && firebase --config ../firebase.json --project archivepilates deploy --only functions,firestore:rules,firestore:indexes"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "firebase-tools": "^15.16.0",
    "typescript": "^5.6.3"
  }
}
