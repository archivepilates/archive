# SOLAPI 템플릿 데이터 연결 운영 규칙

이 문서는 ARCHIVE IN에서 StudioMate/Firebase 데이터를 SOLAPI 카카오 알림톡 템플릿에 연결할 때의 운영 기준이다. 목적은 템플릿별 대상자 선정, 변수 매핑, 제외 조건, 중복 발송 방지를 한 기준으로 맞추는 것이다.

## 기본 원칙

- 알림톡 발송 기준 데이터는 `memberProfiles/{memberId}`를 중심으로 한다.
- 회원 식별은 이름이 아니라 `memberId` 우선, 보조로 `memberPhone`을 사용한다.
- Google 주소록은 보조 저장소이며 알림톡 대상자 원천으로 쓰지 않는다.
- 정산/매출/월간 분석용 엑셀 데이터는 알림톡 대상자 산정에 직접 쓰지 않는다.
- 수강권 기간/횟수 알림톡 대상자 원천은 `memberProfiles.activeTickets`다. 운영자 ACTIONS 날짜 목록은 화면 참고용이며 알림톡 대상자 선정 원천으로 쓰지 않는다.
- 수강권 기간/횟수 알림톡은 현재 사용중인 대상 수강권만 후보로 만들되, 중복/제외 판단에서는 `사용예정` 수강권도 후속 유효 수강권으로 본다. 이미 다음 수강권이 등록되어 있으면 잔여횟수/만료 임박 알림을 보내지 않는다.
- 템플릿 상태가 `APPROVED`가 아니면 실제 발송하지 않고 후보만 생성한다.
- 한 회원에게 같은 날 여러 후보가 생기면 신규회원, 프라이빗 사전설문, 잔여횟수, 잔여기간, 예약안내 순으로 우선순위를 둔다.
- 회원 연락처가 없거나 휴대폰 형식이 아니면 후보는 `skipped`로 기록하고 발송하지 않는다.
- 같은 회원에게 같은 사유의 알림을 반복 발송하지 않는다.
- 자동 발송은 매일 11:30 KST에 실행한다. 운영자가 직접 `queued`로 둔 후보는 큐 워커가 5분 주기로 처리한다.

## 수업/수강권 성격 판단 기준

- `lectures.lessonType`은 수업 자체가 그룹/프라이빗인지 판단하는 원천이다.
- `memberProfiles.activeTickets[].classType`은 회원이 보유한 수강권 성격을 판단하는 원천이다.
- `bookings.lessonType`은 예약 단위에서 수업 성격을 바로 확인하기 위한 캐시 필드다. 알림톡과 사전설문 로직은 `bookings.lessonType`을 먼저 사용한다.
- `bookings.ticketClassType`은 예약 수강권이 회원카드의 활성 수강권과 매칭될 때 저장하는 보조 필드다.
- `ticketName` 문자열 추정은 위 구조화 필드가 비어 있을 때만 사용하는 fallback이다. `순환`, `100회권`, 이벤트권처럼 이름만으로 그룹/프라이빗을 판단하기 어려운 수강권은 문자열 추정에 의존하지 않는다.

## 공통 데이터 매핑

| 템플릿 변수 | 원천 필드 | 변환 규칙 |
| --- | --- | --- |
| `#{이름}` | `memberProfiles.name` | 회원명 그대로 사용하되 공백을 정리한다. |
| `#{수강권명}` | `memberProfiles.activeTickets[].name` | 대상 수강권 1개만 선택한다. 여러 장이면 가장 먼저 만료/소진되는 수강권을 고른다. |
| `#{남은일수}` | `activeTickets[].expiresAt` | 한국시간 오늘 기준 잔여 일수. 오늘 만료는 `0`, 이미 만료는 발송 대상 제외. |
| `#{잔여횟수}` | `activeTickets[].remainingCount` | 숫자만 전달한다. `null` 또는 기간권이면 횟수권 템플릿에서 제외한다. |
| `#{만료일}` | `activeTickets[].expiresAt` | `YYYY. M. D.` 형식으로 표기한다. |
| `#{예약주차}` | 예약 오픈 기준 주차 | 예: `5/18~5/24` 또는 운영자가 확정한 주차 문구. |
| `#{관리번호}` | 강사레슨 수업자료 관리번호 | 버튼 URL `https://in.archivepilates.com/method/#{관리번호}`에 사용한다. |
| `#{미방문일수}` | 마지막 출석 완료일과 발송 기준일 차이 | 숫자만 전달한다. 기준일 포함 없이 지난 일수로 계산한다. |
| `#{마지막출석일}` | `bookings[].lectureDate` 중 마지막 출석 완료일 | `YYYY. M. D.` 형식으로 표기한다. |
| `#{회차}` | 프라이빗 차트 회차 | 회원용 리포트 알림톡에서는 `privateLessonChartRecords.sessionNumber + "회차"`로 전달한다. |
| `#{수업일시}` | 수업 날짜와 시작 시간 | `lessonDate`와 `lessonStartAt`을 KST 기준 `MM.DD 요일 HH:mm` 형식으로 전달한다. |
| `#{강사명}` | 담당 강사명 | `privateLessonChartRecords.staffName`을 사용한다. |
| `#{리포트링크ID}` | 회원용 HTML 리포트 짧은 링크 ID | 버튼 URL `https://in.archivepilates.com/s/#{리포트링크ID}/`에 사용한다. 원본 리포트 URL은 `shortLinks`에 저장한다. |
| `#{인바디링크ID}` | 최신 인바디 리포트 짧은 링크 ID | 버튼 URL `https://in.archivepilates.com/s/#{인바디링크ID}/`에 사용한다. 인바디 리포트가 없으면 측정 데이터 없음 안내 화면으로 연결한다. |

## 템플릿별 운영 규칙

| 템플릿 | Template ID | 현재 상태 | 자동 연결 단계 |
| --- | --- | --- | --- |
| 아카이브 스튜디오메이트 예약 안내 v3 | `KA01TP260518023011547VpbovK8MrI9` | `APPROVED` | 월요일 예약 오픈 안내 후보 |
| 아카이브 신규회원 웰컴 안내 v3 | `KA01TP260514081318309wQGfeIJxIAJ` | `APPROVED` | 매일 11:30 자동 발송 |
| 아카이브 그룹 기간권 잔여기간 안내 v3 | `KA01TP260514145047261araXgWLVFRs` | `APPROVED` | 매일 11:30 자동 발송 |
| 아카이브 그룹 횟수권 잔여횟수 안내 v3 | `KA01TP260514145047393VpTbcCZKkCV` | `APPROVED` | 매일 11:30 자동 발송 |
| 아카이브 프라이빗 횟수권 잔여횟수 안내 v1 | `KA01TP260514152235608d9icGOBotnV` | `APPROVED` | 매일 11:30 자동 발송 |
| 아카이브 프라이빗 기간권 잔여기간 안내 v1 | `KA01TP260514153314927WH270IppWQS` | `APPROVED` | 매일 11:30 자동 발송 |
| 아카이브 프라이빗 사전설문 안내 v1 | `KA01TP260514153632171uiWXYoeiOLS` | `APPROVED` | 매일 11:30 자동 발송 |
| 아카이브 장기 미방문 수업안내 v1 | `KA01TP260524083643752cySb9BoDOjN` | `PENDING` | 후보 생성 연결 완료, 승인 전 실제 발송 차단 |
| 강사레슨_수업자료 안내 v1 | `KA01TP260521120040094XcMvYgFTryj` | `APPROVED` | 아카이브강사레슨 채널, 수업자료/방문안내 버튼 2개 구성 |
| 회원용_프라이빗 수업 리포트 안내 v2 | `KA01TP260528090731992hVPP5efmmUC` | `PENDING` | 수업 리포트와 최신 인바디 리포트를 버튼 2개로 통합 발송. 검수 승인 후 사용 |
| 회원용_프라이빗 수업 리포트 안내 v1 | `KA01TP260528081225871Fr92FW901Vo` | `INSPECTING` | v2로 대체 예정. 승인되더라도 신규 운영 연결은 v2 기준 |
| 회원용_인바디 리포트 안내 v1 | `KA01TP260528090148593isshfXtt8vE` | `PENDING` | 단독 인바디 발송용으로 보류. 프라이빗 리포트 운영은 v2 통합 기준 |
| 회원용_수업 일정 변경 안내 v1 | `KA01TP2607050825471794qyKK2E0URD` | `INSPECTING` | 운영 공지 수동 발송용. 승인 전 실제 발송 금지 |
| 회원용_강사 변경 안내 v1 | `KA01TP2607050825481844FfRze7o9Pw` | `INSPECTING` | 운영 공지 수동 발송용. 승인 전 실제 발송 금지 |
| 회원용_센터 운영 휴무 안내 v1 | `KA01TP260705082548935P3S0jGYX6Oz` | `INSPECTING` | 운영 공지 수동 발송용. 승인 전 실제 발송 금지 |

### 1. 신규회원 웰컴 안내

대상:

- StudioMate 회원 등록일이 확인된 신규회원.
- 기존 `memberProfiles`에 동일 `memberId` 기록이 없던 회원.
- 등록일 기준 3일 이내.
- 알림톡 자동 발송은 2026-05-16 이후 등록 회원부터 적용한다. 2026-05-15 이전 등록자는 backlog 발송하지 않는다.
- 신규회원 웰컴 후보는 발송 기준일 기준 등록 3일 이내 회원만 만든다. 3일이 지나면 후보 생성/자동 큐 전환 대상에서 제외한다.
- 활성 수업 수강권을 1개 이상 보유한 회원.
- 연락처가 정상 저장된 회원.

발송 시점:

- StudioMate 등록 감지 후 10분 이내 후보 생성.
- 매일 11:30 자동 발송 대상에 포함한다.

제외:

- 전화번호 없음.
- 활성 수업 수강권 없음. 상담고객처럼 회원 등록만 있고 수강권이 없는 경우는 제외.
- 체험권/그룹체험권만 보유한 회원. 체험권은 정규 수강권 안내 대상이 아니다.
- 강사레슨 수강권만 보유한 회원. 강사레슨은 강사/스탭 운영용 예외 수강권으로 보고 일반 회원 알림톡 대상에서 제외한다.
- 출산예정 등 운영자가 개별 예외로 지정한 회원.
- 기존 회원으로 확인됨.
- 이미 신규회원 웰컴 알림톡 발송 이력이 있음.
- 테스트/직원/미수금 등 운영 제외 태그가 있는 회원.

중복키:

- `new_member:{memberId}`

중복 방지 기간:

- 영구 1회. 같은 회원에게 신규회원 웰컴은 다시 보내지 않는다.

### 2. 그룹 기간권 잔여기간 안내

대상:

- 활성 그룹 기간권을 보유한 회원.
- 만료일까지 14일 이내.
- 이미 만료되지 않은 수강권.
- 같은 용도의 다른 현재 또는 사용예정 그룹 수강권을 보유하지 않은 회원.

발송 기준:

- `남은일수`가 `14, 7, 3, 1` 중 하나일 때 후보를 만들 수 있다.
- 초기 운영은 `14일 이내 최초 1회`만 발송한다.

제외:

- 다른 현재 또는 사용예정 그룹 수강권이 이미 있음.
- 수강권이 환불/사용불가/만료 상태.
- 회원이 프라이빗만 이용 중인 경우.
- 최근 7일 내 동일 수강권 만료 안내 발송 이력이 있음.

중복키:

- `group_period_expiring:{memberId}:{ticketId}:{expiresAt}`

중복 방지 기간:

- 30일. 같은 회원, 같은 템플릿, 같은 수강권으로 성공 발송된 이력이 최근 30일 안에 있으면 차단한다.

### 3. 그룹 횟수권 잔여횟수 안내

대상:

- 활성 그룹 횟수권을 보유한 회원.
- 잔여횟수 5회 미만, 즉 `1~4회`.
- 같은 용도의 다른 현재 또는 사용예정 그룹 수강권을 보유하지 않은 회원.

발송 기준:

- `잔여횟수`가 `4, 3, 2, 1` 중 하나일 때 후보를 만들 수 있다.
- 초기 운영은 `5회 미만 최초 1회`만 발송한다.

제외:

- 잔여횟수 0회.
- 다른 현재 또는 사용예정 그룹 수강권이 이미 있음.
- 기간권처럼 잔여횟수가 없는 상품.
- 수강권이 환불/사용불가/만료 상태.

중복키:

- `group_count_low:{memberId}:{ticketId}:{remainingCount}`

중복 방지 기간:

- 30일. 같은 회원, 같은 템플릿, 같은 수강권으로 성공 발송된 이력이 최근 30일 안에 있으면 차단한다.

### 4. 프라이빗 횟수권 잔여횟수 안내

대상:

- 활성 프라이빗 횟수권을 보유한 회원.
- 잔여횟수 3회 이하, 즉 `1~3회`.
- 프라이빗 수업 이용 이력이 있거나 예약이 있는 회원.

발송 기준:

- `잔여횟수`가 `3, 2, 1` 중 하나일 때 후보를 만들 수 있다.
- 초기 운영은 `3회 이하 최초 1회`만 발송한다.

제외:

- 잔여횟수 0회.
- 다른 현재 또는 사용예정 프라이빗 수강권이 이미 있음.
- 그룹권만 보유한 회원.
- 수강권이 환불/사용불가/만료 상태.

중복키:

- `private_count_low:{memberId}:{ticketId}:{remainingCount}`

중복 방지 기간:

- 30일. 같은 회원, 같은 템플릿, 같은 수강권으로 성공 발송된 이력이 최근 30일 안에 있으면 차단한다.

### 5. 프라이빗 사전설문 안내

대상:

- 프라이빗 수강권을 처음 등록한 회원.
- 또는 첫 프라이빗 예약이 생성된 회원.
- 설문 발송 이력이 없는 회원.

발송 시점:

- 프라이빗 수강권 등록 감지 후 후보 생성.
- 첫 프라이빗 수업 시작 전까지 발송하는 것을 원칙으로 한다.
- 현재 자동화는 예약 가능 기간 안의 첫 프라이빗 예약을 기준으로 후보를 만든다.
- 강사용 설문 전달 기준시각은 수업 하루 전 오전 9시다.
- 강사용 전달 기준시각까지 회원 설문 제출이 없으면 `home@archivepilates.com`으로 미제출 알림 메일을 1회 발송한다.

제외:

- 이미 설문 발송 이력 있음.
- 기존 프라이빗 이용 이력이 있음.
- 전화번호 없음.
- 프라이빗 수업이 취소되었고 향후 예약이 없음.

중복키:

- `private_survey:{memberId}`

중복 방지 기간:

- 영구 1회. 같은 회원에게 프라이빗 사전설문은 다시 보내지 않는다.
- 설문 응답 원본은 `privateSurveyResponses/{responseId}`를 최초 생성 1회만 허용한다. 같은 `responseId` 재제출은 중복으로 처리하고 강사용 알림톡/메모 큐를 다시 만들지 않는다.

### 5-1. 그룹 첫 수업 사전확인 안내

대상:

- ARCHIVE PILATES 그룹수업이 처음인 회원.
- 예약 가능 기간 안에 첫 그룹수업 예약이 있는 회원.
- 기존 그룹 사전확인 설문 제출 이력이 없는 회원.

발송 시점:

- 회원용 설문 요청은 첫 그룹수업 전 예약 기준으로 발송한다.
- 강사용 설문 전달 기준시각은 수업 시작 1시간 전이다.
- 강사용 전달 기준시각까지 회원 설문 제출이 없으면 `home@archivepilates.com`으로 미제출 알림 메일을 1회 발송한다.

중복 방지:

- `groupSurveyRequests/{requestId}`가 `submitted` 상태이면 설문 페이지와 API 모두 추가 제출을 받지 않는다.
- 설문 응답 원본은 `privateSurveyResponses/{responseId}`를 최초 생성 1회만 허용한다. 같은 `responseId` 재제출은 중복으로 처리하고 강사용 알림톡/메모 큐를 다시 만들지 않는다.

버튼 URL 제한:

- SOLAPI 버튼 URL은 변수 치환 후 100자 이하여야 한다.
- 함수는 원본 링크를 `shortLinks/{링크ID}`에 저장하고, 버튼에는 `https://in.archivepilates.com/s/#{링크ID}/` 형식의 짧은 링크를 넣는다.
- 회원용 그룹 설문 원본 URL은 `https://in.archivepilates.com/groupSurvey?id=#{설문ID}&token=#{접근토큰}` 형식이다.
- 강사용 설문 확인 원본 URL은 `https://in.archivepilates.com/privateSurveyResponseView?id=#{설문ID}&token=#{접근토큰}` 형식이다.
- 현재 승인 템플릿이 기존 변수 버튼을 쓰는 동안에는 원본 URL과 짧은 URL을 모두 검사한다.
- 짧은 링크 생성 전후에도 URL이 100자를 넘으면 ARCHIVE IN이 SOLAPI 발송 전에 차단한다.

### 6. 장기 미방문 수업안내

대상:

- 활성 수업 수강권을 보유한 회원.
- 마지막 출석 완료일이 발송 기준일로부터 7일 이상 지난 회원.
- 회원 연락처가 정상 저장된 회원.
- 마지막 출석일과 대상 수강권명이 확인되는 회원.

발송 시점:

- 매일 11:30 자동 후보 재계산 대상에 포함한다.
- 템플릿이 `APPROVED`가 되기 전에는 후보만 생성하고 실제 발송은 차단한다.

제외:

- 수강권 상태가 `정지중`, `중지`, `홀딩`인 회원. StudioMate 엑셀의 `수강권상태`를 `memberProfiles.ticketStatusSummary.hasHoldingTicket`으로 저장해 판단한다.
- 활성 수업 수강권 없음.
- 스텝 계정.
- 출석 완료 이력 없음.
- 발송 기준일 이후 예정 예약 있음.
- 마지막 출석 완료일이 7일 미만.
- 강사레슨 수강권 또는 수업권이 아닌 상품만 보유.
- 최근 14일 내 같은 장기 미방문 안내 성공 발송 이력 있음.
- SOLAPI 미승인 템플릿.

중복키:

- `long_absence:{memberId}`

중복 방지 기간:

- 14일. 같은 회원에게 장기 미방문 안내가 최근 14일 안에 성공 발송되었으면 차단한다.

### 7. 예약 안내

대상:

- 예약 오픈 안내가 필요한 활성 이용회원.
- 예약 오픈 주간에 유효한 그룹 또는 혼합 수강권을 보유한 회원.
- 수강권 시작일이 예약 오픈 주간 마지막 날 이내이고, 만료일이 예약 오픈 주간 시작일 이후인 회원.

발송 시점:

- 매주 월요일 12:30 KST에 예약 안내 전용 루틴으로 자동 후보를 생성한다.
- 매일 11:30 KST 전체 알림톡 루틴에서는 예약 안내 후보를 생성하지 않는다.
- 발송 대상이 10건 이상이면 대량 발송 승인 메일을 먼저 보내고, 승인 후 큐 전환/발송한다.

제외:

- 활성 그룹/혼합 수강권 없음.
- 프라이빗 수강권만 보유.
- 강사레슨 수강권 보유 대상.
- 수강권 시작일이 예약 오픈 주간 마지막 날 이후.
- 수강권 만료일이 예약 오픈 주간 시작일 이전.
- 최근 동일 예약주차 안내 발송 이력 있음.

중복키:

- `reservation_open:{memberId}:{reservationWeek}`

중복 방지 기간:

- 6일. 예약 오픈 안내는 주마다 반복될 수 있으므로 같은 내용은 한 주 안에서만 차단한다. `예약주차`가 바뀌면 다른 내용으로 본다.

### 8. 강사레슨 수업자료 안내

대상:

- 아카이브강사레슨 채널로 안내해야 하는 강사레슨 예약 회원.
- 수업에 연결된 ARCHIVE METHOD 교재 관리번호가 있는 예약.
- 회원 연락처가 정상 저장된 회원.

발송 시점:

- 기본은 수업 하루 전 알림톡 발송.
- 템플릿이 `APPROVED`가 되기 전에는 후보를 만들더라도 실제 발송하지 않는다.

버튼 URL:

- `수업자료 보기`
- `https://in.archivepilates.com/s/#{링크ID}/` 형식의 짧은 링크를 사용한다.
- 원본 자료 URL은 `https://in.archivepilates.com/method/#{관리번호}`이다.
- 관리번호는 `영문수업주제-YYMMDD` 형식으로 정규화한다. 수업명, 기존 링크, 테스트 후보에 `kg02`, `bm01`, `jy03` 같은 회원/테스트 식별자가 섞여 있으면 영문 주제와 날짜만 남긴다.
- 예: `circulation-kg02-260530` -> `circulation-260530`
- 같은 수업자료 단축링크는 여러 후보가 공유할 수 있으므로 `shortLinks.sourceIds`에 사용 후보를 누적하고, 반복 발송 때 기존 `sourceId`를 덮어쓰지 않는다.
- `방문안내 보기`
- `https://www.notion.so/367d49eae4bf811ca3daea273ed278c8`
- 버튼 URL은 변수 치환 후 100자 이하여야 한다. 현재 승인 템플릿이 기존 `관리번호` 버튼을 쓰는 동안에는 원본 자료 URL도 함께 검사한다.

제외:

- 관리번호가 비어 있는 예약.
- 버튼 URL이 변수 치환 후 100자를 초과하는 예약.
- 강사레슨 예약이 취소된 회원.
- 같은 수업/관리번호 조합으로 이미 발송된 이력이 있는 회원.

중복키:

- `instructor_lesson_material:{memberId}:{lessonDate}:{managementNumber}`

중복 방지 기간:

- 영구 1회. 같은 회원, 같은 수업일, 같은 관리번호 조합으로 중복 발송하지 않는다.

### 9. 회원용 프라이빗 수업 리포트 안내

용도:

- 강사용 입력 요청이 아니라, 수업 후 생성된 회원용 HTML 리포트를 회원에게 전달하는 알림톡이다.
- 발송은 자동 즉시 발송이 아니라 Notion 검수 후 `발송` 체크가 된 회차만 처리한다.
- v2부터는 같은 알림톡에 최신 인바디 리포트 버튼을 함께 제공한다.

대상:

- `privateLessonChartRecords/{recordId}`에 해당 회차 기록이 있음.
- `postRecord`가 제출되어 있음.
- `gptStatus`가 `draft_created` 상태임. 기존 속성명은 유지하지만 생성 주체는 Gemini API다.
- `publicReportUrl` 또는 회원 리포트 URL이 존재함.
- Notion 세션 기록의 `발송` 체크박스가 체크되어 있음.
- Notion 세션 기록의 `발송상태`가 `대기`임.
- 회원 전화번호가 정상 저장되어 있음.
- 같은 `recordId`로 회원용 리포트 알림톡 성공 발송 이력이 없음.

발송 시점:

- 수업 후 리포트 생성.
- 운영자 또는 강사가 Notion에서 리포트를 검수.
- Notion `발송` 체크 후 Notion 웹훅이 Firebase Function으로 전달된다.
- Function은 해당 Notion 페이지를 다시 조회해서 `발송=true`, `발송상태=대기`, `회원 리포트` URL 조건을 확인한 뒤 알림톡 큐로 전환한다.
- 웹훅 누락 복구용으로 하루 3회만 동일 조건을 재조회한다.

변수:

- `#{회원명}` = `privateLessonChartRecords.memberName`
- `#{회차}` = `privateLessonChartRecords.sessionNumber + "회차"`
- `#{수업일시}` = `lessonDate + lessonStartAt`을 KST 기준으로 변환
- `#{강사명}` = `privateLessonChartRecords.staffName`
- `#{리포트링크ID}` = `privateLessonChartRecords.publicReportUrl`을 `shortLinks`로 변환한 짧은 링크 ID
- `#{인바디링크ID}` = 최신 `inbodyWebhookEvents.memberReportUrl`을 `shortLinks`로 변환한 짧은 링크 ID. 없으면 측정 데이터 없음 안내 화면 링크 ID

버튼:

- `수업 리포트 보기`
  - URL 변수: `https://in.archivepilates.com/s/#{리포트링크ID}/`
- `인바디 리포트 보기`
  - URL 변수: `https://in.archivepilates.com/s/#{인바디링크ID}/`
- 실제 발송 payload에는 전체 URL이 아니라 `pr-...` 형식의 `#{리포트링크ID}`와 `ir-...` 형식의 `#{인바디링크ID}`만 넣는다.
- 원본 `publicReportUrl`은 Firestore `shortLinks/{리포트링크ID}.targetUrl`에 저장한다.
- 최신 인바디 리포트 URL은 `shortLinks/{인바디링크ID}.targetUrl`에 저장한다.
- 인바디 측정 데이터가 없으면 `https://in.archivepilates.com/reports/inbody-members/?status=no-data&member=...` 안내 화면으로 연결한다.

제외:

- 회원 전화번호 없음.
- `publicReportUrl` 없음.
- Gemini 회원용 초안 미생성.
- 수업 후 기록 미제출.
- Notion `발송` 체크 전.
- Notion `발송상태`가 `대기`가 아님.
- 발송상태가 완료, 실패 후 수동 보류, 또는 운영 보류 상태.
- 동일 회차 `recordId`로 이미 발송 완료된 경우.

중복키:

- `private_lesson_report:{recordId}`

중복 방지 기간:

- 영구 1회. 같은 회차 리포트는 같은 회원에게 다시 발송하지 않는다.

중복 방지 정책:

- Notion 웹훅 이벤트 ID는 `notionWebhookEvents/{eventId}`에 기록해서 같은 이벤트 재전송을 무시한다.
- 알림톡 후보 ID는 `private_lesson_report_{recordId}`로 고정한다.
- 후보가 이미 `queued` 또는 `processing`이면 새 후보를 만들지 않는다.
- 후보가 이미 `sent`이면 Notion `발송상태`를 `완료`로 맞추고 추가 발송하지 않는다.
- 실패 후보는 Notion 상태가 계속 `대기`일 때만 같은 후보 ID로 재시도 가능하게 둔다.

### 10. 회원용 인바디 리포트 안내

용도:

- 프라이빗 유효회원이 인바디 측정 후 생성된 개인 리포트를 알림톡으로 전달한다.
- 리포트는 Firebase Hosting의 개인별 URL로 열고, 알림톡 버튼에는 짧은 링크 ID만 전달한다.

대상:

- 프라이빗 유효회원.
- 인바디 측정 데이터가 있고 회원용 리포트 URL이 생성됨.
- 회원 전화번호가 정상 저장되어 있음.
- 같은 측정 또는 같은 리포트에 대해 성공 발송 이력이 없음.
- 운영자 승인 또는 자동 발송 조건을 충족함.

발송 시점:

- 인바디 리포트 URL 생성 후 후보를 만든다.
- 초기 운영은 운영자 승인 후 발송으로 둔다.
- 자동 발송으로 전환할 때는 프라이빗 유효회원 여부와 중복키 검증을 먼저 통과해야 한다.

변수:

- `#{회원명}` = 회원 이름
- `#{리포트링크ID}` = `shortLinks`에 저장된 인바디 리포트 짧은 링크 ID

버튼:

- `리포트 확인하기`
- URL 변수: `https://in.archivepilates.com/s/#{리포트링크ID}/`
- 원본 URL 예시: `https://in.archivepilates.com/reports/inbody-members/{reportToken}`
- 실제 발송 payload에는 전체 URL이 아니라 `ir-...` 형식의 `#{리포트링크ID}`만 넣는다.
- 원본 리포트 URL은 Firestore `shortLinks/{리포트링크ID}.targetUrl`에 저장한다.

제외:

- 프라이빗 유효회원 아님.
- 회원 전화번호 없음.
- 인바디 리포트 URL 없음.
- 같은 측정 또는 같은 리포트 발송 이력 있음.
- 운영자 승인 전 또는 자동 발송 조건 미충족.
- SOLAPI 미승인 템플릿.

중복키:

- 기본: `inbody_report:{reportToken}`
- 측정일 기준 후보에서는 `inbody_report:{memberId}:{latestMeasurementDateTime}`도 허용한다.

중복 방지 기간:

- 영구 1회. 같은 리포트 토큰 또는 같은 측정 시각 리포트는 다시 발송하지 않는다.

### 12. 운영 공지 알림톡

용도:

- 수업 일정 변경, 강사 변경, 센터 운영/휴무처럼 회원의 서비스 이용에 직접 필요한 공지를 발송한다.
- 이벤트, 홍보, 프로모션, 신규 프로그램 안내는 운영 공지 알림톡으로 보내지 않는다.
- 노션은 공지 작성/보관용으로 사용하고, 실제 대상 선택/테스트 발송/운영자 승인/실제 발송/로그 저장은 ARCHIVE CORE에서 처리한다.

템플릿:

- `회원용_수업 일정 변경 안내 v1`
  - Template ID: `KA01TP2607050825471794qyKK2E0URD`
  - 변수: `#{회원명}`, `#{수업명}`, `#{기존일시}`, `#{변경일시}`, `#{공지ID}`
- `회원용_강사 변경 안내 v1`
  - Template ID: `KA01TP2607050825481844FfRze7o9Pw`
  - 변수: `#{회원명}`, `#{수업명}`, `#{수업일시}`, `#{기존강사}`, `#{변경강사}`, `#{공지ID}`
- `회원용_센터 운영 휴무 안내 v1`
  - Template ID: `KA01TP260705082548935P3S0jGYX6Oz`
  - 변수: `#{회원명}`, `#{공지제목}`, `#{적용기간}`, `#{운영안내}`, `#{공지ID}`

버튼:

- 버튼명: `공지 자세히 보기`
- URL 변수: `https://in.archivepilates.com/notice/#{공지ID}`
- 원본 노션 URL은 별도 `noticeLinks` 또는 동등한 중계 컬렉션에 저장하고, 알림톡 버튼에는 ARCHIVE 도메인 중계 링크만 사용한다.

대상 선택:

- 수업 일정 변경: 특정 수업 예약자.
- 강사 변경: 특정 수업 예약자 또는 특정 강사 수업 회원.
- 센터 운영/휴무: 전체 유효회원 또는 운영 영향이 있는 특정 대상군.

발송 절차:

- 노션 상위 페이지에서 공지 상세 페이지를 작성한다.
- ARCHIVE CORE에서 템플릿과 공지 URL을 선택한다.
- 김기효 테스트 발송을 먼저 수행한다.
- 운영자 승인 후 실제 발송한다.
- 발송 결과와 실패 사유는 `alimtalkCandidates`, `alimtalkSends`, 운영 로그 문서에 남긴다.

제외:

- SOLAPI 템플릿 상태가 `APPROVED`가 아닌 경우.
- 공지 상세 링크가 없거나 `공지ID`가 생성되지 않은 경우.
- 운영자 승인 전.
- 이벤트/홍보/프로모션 성격의 공지.
- 같은 공지ID와 같은 대상자에게 성공 발송 이력이 있는 경우.

중복키:

- `notice:{noticeId}:{templateCode}:{memberId}`

중복 방지 기간:

- 같은 공지ID 기준 영구 1회. 같은 공지를 같은 회원에게 다시 보내지 않는다.

## 후보 상태 운영

| 상태 | 의미 | 처리 |
| --- | --- | --- |
| `candidate` | 규칙에 의해 생성된 후보 | 운영자 화면에 표시 |
| `reviewed` | 운영자가 확인했지만 아직 발송 전 | 발송 대기 또는 보류 선택 |
| `queued` | 발송 큐에 들어감 | 재시도/결과 대기 |
| `sent` | SOLAPI 발송 성공 | 발송 이력과 회원카드에 기록. ACTIONS에서는 완료 뱃지로 강조하지 않는다. |
| `skipped` | 제외 처리 | 사유를 기록하고 같은 조건 반복 노출 방지 |
| `failed` | 발송 실패 | 에러와 재시도 가능 여부 기록 |

## 발송 전 체크

자동 또는 버튼 발송 직전 아래 조건을 모두 통과해야 한다.

1. 템플릿 상태가 `APPROVED`이다.
2. 회원 연락처가 있고 휴대폰 번호로 정규화되어 있다.
3. StudioMate 동기화 데이터가 최신이다.
4. 같은 `dedupeKey`로 템플릿별 중복 방지 기간 안의 성공 발송 이력이 없다.
5. 대상 수강권이 아직 활성 상태다.
6. 다른 현재 또는 사용예정 유효 수강권 보유로 제외되는 케이스가 아니다.
7. 운영자가 `skipped` 처리한 후보가 아니다.

## 우선순위

같은 회원에게 같은 날 여러 후보가 생성되면 아래 순서로 하나만 기본 추천한다.

1. 신규회원 웰컴
2. 프라이빗 사전설문
3. 프라이빗 잔여횟수
4. 그룹 잔여횟수
5. 그룹 잔여기간
6. 장기 미방문
7. 예약 안내
8. 강사레슨 수업자료 안내

운영자가 직접 선택하면 같은 날 여러 건을 발송할 수 있지만, 기본 자동 추천은 1건으로 제한한다.

## 자동 발송 스케줄

- `scheduledQueueAndSendAlimtalkDaily`: 매일 11:30 KST.
- `scheduledQueueAndSendReservationOpenAlimtalk`: 매주 월요일 12:30 KST. 수업예약오픈안내 전용 루틴이며, 유효 그룹/혼합 수강권 대상자만 계산한다.
- 실행 시 `memberProfiles.activeTickets`를 직접 스캔해 발송 기준일의 수강권 상태를 재계산한다.
- 수강권 기간/횟수 안내는 발송 기준일 기준 만료 14일 이내, 그룹 잔여 5회 미만, 프라이빗 잔여 3회 이하 후보만 `queued`로 전환한다.
- 프라이빗 사전설문은 2026-05-19 이후 프라이빗 수강권 시작일이 발송 기준일과 같은 회원만 후보로 만든다. 기존 프라이빗 회원 전체에게 소급 발송하지 않는다.
- 장기 미방문 안내는 2026-05-24 이후 활성 수업 수강권 보유, 마지막 출석 완료 7일 이상, 수강권 정지/중지/홀딩 아님, 발송 기준일 이후 예정 예약 없음 조건으로 후보를 만든다.
- 운영자 ACTIONS에 생성된 과거/미래 날짜 후보는 알림톡 발송 원천으로 쓰지 않는다. 예전 대상일에 생성된 후보는 현재 수강권 상태와 맞지 않을 수 있으므로 자동 발송하지 않는다.
- 신규회원 웰컴은 발송 기준일 기준 등록 3일 이내 후보만 `queued`로 전환한다.
- 전환 후 큐를 즉시 처리한다. 잔여 `queued` 후보는 `scheduledProcessAlimtalkQueue`가 5분 주기로 이어서 처리한다.
- 후보 선정 단계에서 먼저 `memberId/memberPhone + templateCode + 발송범위` 기반 `dedupeKey`로 성공 발송 이력을 확인한다. 수강권 알림의 발송범위는 수강권명이고, 날짜별 남은일수/잔여횟수 변화는 같은 내용으로 본다.
- 중복이면 발송 가능 후보 ID에 넣지 않는다. 기존 후보 문서가 있거나 당일 리포트에 표시해야 하는 경우 `skipped`로 바꾸며 사유를 `중복 발송 차단`으로 남긴다.
- SOLAPI 발송 직전에도 같은 중복 검사를 다시 수행해 동시 실행이나 데이터 변경으로 인한 중복 발송을 차단한다.
- 템플릿별 중복 방지 기간은 예약 오픈 안내 6일, 수강권 기간/횟수 안내 30일, 장기 미방문 안내 14일, 신규회원 웰컴/프라이빗 사전설문 영구 1회다.
- 운영자 실발송 테스트 수신자 `김기효 / 01086488585 / memberId 1982133`은 스텝 계정 제외와 중복 발송 차단에서 제외한다. 이 예외는 테스트 수신자 단일 건에만 적용하고, 다른 스텝/회원에게 확대하지 않는다.

## 기록해야 할 데이터

`alimtalkCandidates/{candidateId}`:

- `memberId`, `memberName`, `memberPhone`
- `templateCode`
- `reason`
- `payload`
- `dedupeKey`
- `sourceDataVersion`
- `sourceSyncedAt`
- `status`
- `reviewedByUid`, `reviewedAt`

`alimtalkSends/{sendId}`:

- `candidateId`
- `templateCode`
- `payload`
- `solapiMessageId`
- `status`
- `attempts`
- `lastError`
- `sentAt`

회원카드:

- 최근 발송 템플릿
- 발송일
- 발송 사유
- 실패/제외 사유

## 운영자 화면 표시 기준

운영자 ACTIONS에는 단순히 "자동대상"으로 묶지 않고 아래처럼 사유를 분류해 보여준다.

- 신규회원
- 그룹 기간 임박
- 그룹 횟수 부족
- 프라이빗 횟수 부족
- 프라이빗 설문 필요
- 예약 안내 대상

각 후보 카드에는 `템플릿명`, `선정 이유`, `수강권명`, `남은일수/잔여횟수`, `최근 동기화 시각`, `발송/제외 버튼`을 표시한다.

## 현재 코드와 맞춰야 할 수정 포인트

- `rebuildAlimtalkCandidates.ts`의 템플릿 ID를 최신 승인 템플릿 기준으로 분기해야 한다.
- 그룹/프라이빗 수강권을 구분해 기간권과 횟수권 규칙을 따로 적용해야 한다.
- `ticket_expiring` 하나로 뭉친 후보를 `group_period_expiring`, `group_count_low`, `private_count_low`처럼 사유별로 분리해야 한다.
- 신규회원 웰컴 v3는 `APPROVED` 상태이므로 자동 후보에 연결할 수 있다.
- 검수중 템플릿은 후보만 생성하고 실제 발송 버튼은 비활성화한다.
- 프라이빗 기간권 템플릿은 운영 보류 상태로 두고 자동 연결하지 않는다.
