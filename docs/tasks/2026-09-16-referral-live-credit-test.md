# ARCHIVE PILATES referral credit test

- Approval: two existing ordinary test members, KRW 3,000 each, duplicate check and full compensation. No real customer, order, group, classroom or production policy writes.
- Source: complete Imweb member scan, 83 members / 2 pages; exact historical pair resolved from private canonical hashes and verified against production exclusions.
- Inviter: preserve historical paid-and-compensated referral row. Current worker skips it without sending. Separately labelled approved manual test credit of KRW 3,000 verifies the recipient's real point balance without bypassing the canonical duplicate guard.
- Invitee: current role-specific worker issues and verifies exactly one KRW 3,000 reward using native point logs. The private test policy starts at the canonical historical signup time; production activation cutoffs and production ledger are unchanged.
- Both current workers rerun with payout preparation prohibited: one duplicate each, zero send attempts, zero failures.
- Both accounts: 0 -> 3,000 -> 0; one new credit and one compensation each. Groups unchanged. Historical and new paid rows retained after compensation.
- Evidence: owner-only journal and ledger backup in `~/ArchiveIN/automation/referral-tests/both-20260916/`. No member identifiers, credentials or native logs in Git.
- Test runner uses an exclusive test lock, durable atomic journal writes, native dry-run/confirmation, single-use attempts and canonical-identity recovery independent of mutable referral attribution.
- Checks: 86 focused worker/both-party/proof tests passed; syntax check passed; read-only peer review passed after durability and recovery corrections.
- Boundary: this is not a new-signup-to-both-first-awards E2E claim. Browser signup consent/prefill proof remains separate. No browser opened for this API/ledger test.
- CORE operating-rule verification entry updated. Only the operating-rule HTML needs publication; reward implementation and Imweb loader remain unchanged.
