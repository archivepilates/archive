# Public Website UX 3-7

- Scope: community direct access, video discovery and previews, consistent headers, photo-first homepage content, Knitido filtering and image delivery.
- Base: origin/codex/mini/imweb-support-movement-paid-20260904 at 87a2e77. This source matches live official-home assets. origin/main does not contain official-home.
- Excluded: unpublished ed4f10d workflow changes, classroom loader and access rules, Functions, product stock/prices, orders and member data.
- Worktree: codex/mini/public-site-ux-20260905.
- Release target: Firebase archive-pilates / archive-pilates-home and scoped Imweb common-code UI loaders.
- Verification: responsive public pages at 320/390/768/1440, filters and preview identity, ordinary authorized and unauthorized classroom accounts, classroom asset hash, live script readback.
- Implemented: five scoped UI enhancements, direct board route, responsive image variants and guarded installers.
- Verified before release: 20 public route/viewport combinations, video and Knitido filters/reset/jump, matching ACH9 preview. Static deployment/classroom guards pass. Classroom and loader source are byte-identical to 87a2e77.
- Ordinary-member baseline: all four role/viewport combinations have exact expected lists (buyer six, nonbuyer zero); all 24 protected-page access checks match. Native paid widgets defer the iframe until play, so verification recognizes the visible native player launch control without claiming playback.
- Baseline warning: Imweb's existing header_more_menu.js calculateMenuWidth occasionally raises querySelector TypeError on desktop private pages. It also occurs before this release and does not change the verified access result. The regression helper records this exact known warning separately; any new error remains a failure. No production error suppression or auth code change.
- CORE rules: no operational policy change; existing ordinary-member verification rule remains in force.
- Official-home deployment: complete on 2026-09-05 KST. Firebase project archive-pilates, Hosting site archive-pilates-home only, service account archive-codex-operator@archive-pilates.iam.gserviceaccount.com, source commit d628dcb.
- Live official verification: root HTML and new UI assets return 200 with SHA-256 matching Git source; /community, /community/ and /community/index.html return 302 to the Imweb community board. Screenshots checked at 320/390/768/1440. Existing classroom asset hash and anonymous-access canary pass.
- Imweb installation: saved on 2026-09-06 KST through the user-approved administrator SEO screen as home@archivepilates.com. CLI safety limits were not bypassed. The SEO common-code fields are distinct from the unit script API fields; do not replace one with the other.
- Saved scope: append the public UX installer to the current SEO Header Code (26062 -> 26739 characters initially, 26831 after the final image adapter); patch only the three Knitido story image URLs and responsive attributes in the current SEO Footer Code (54040 -> 54568 characters). Preserve newer community-menu code and all other content. Header Code top remains unchanged. Reloaded the administrator screen and compared both complete editor values exactly with the prepared UI changes.
- Unit-script protection: all three API script fields still match their before snapshots byte-for-byte, including the body/classroom loader. Public HTML independently confirms the new installer and three WebP URLs. An inert JSON script marker precedes the obsolete loader and satisfies its duplicate check, retiring the missing shipping-review asset request without changing unit code or shipping policy.
- Repeat-run guard: prepare/apply scripts now stop before writing if the public UX installer is already live. Both guards were executed and confirmed to refuse duplicate installation. Do not run old unit payloads over the live SEO installation.
- Final live QA: 20/20 public route/viewport combinations pass without horizontal overflow or page errors. Ordinary-member mobile/desktop matrix passes: buyer six expected cards, nonbuyer zero, 24/24 protected-page checks; both temporary test-group snapshots restored and independently verified. The exact pre-existing desktop header_more_menu.js warning remains separate from access-check failures. Actual payment, refund and video playback were not performed.
- Image runtime correction: network QA found a legacy unit renderer rebuilding the three story photos with external JPGs after the SEO footer patch. Added imweb-knitido-images-20260906.js, scoped to the exact three story image alt texts on /16 and /shop. Its observer loads before the legacy renderer (parser-blocking installer tag); a deferred tag still caused early JPG requests and was corrected before closeout. Final live checks at all four widths show 12/12 loaded WebP photos, zero obsolete loader requests and zero legacy JPG requests. The regression now fails if either obsolete request returns.
- Additional Hosting-only deployment: source commit ae57c83, 2026-09-06 KST, Firebase archive-pilates / archive-pilates-home. Uploaded one new asset, with all configured predeploy checks and postdeploy classroom canary passing. The image adapter returns HTTP 200 and matches source SHA-256 3cbafa5c8c8ba27577c72b8638aff6dbc643a4cdc5bde62c1c70e586b5e9306d. No Functions or database deployment.
- Final rerun after the parser-order correction: public matrix 20/20 passed at 02:03 KST; image network regression 4/4 widths passed with the explicit zero-JPG assertion enabled. Static installer validation and git diff --check pass.
- Browser cleanup: final administrator/profile tabs and all verification Playwright contexts were closed. One earlier login handoff tab (2059950102, Chrome archive profile, task group "사이트 UI 저장") could not be closed through the detached extension. Native closure had requested deletion of the synced group, so it was cancelled; later native UI was unavailable. Preserved this tab/group rather than delete synced state. Other pre-existing Chrome tabs were untouched.
- Release report: docs/reports/2026-09-05-public-site-ux-release.html. CORE rules update not needed because this release changes presentation/navigation only and preserves existing verification and access policy.

## September 6 Instructor Correction

- Operator confirmed AB6/32, ACH6/33 and AB5/45 are all Minjin lessons. Corrected their discovery catalog labels and product summary/detail instructor text to `민진쌤`.
- Product source readback: only `simpleContent`, `content` and `editTime` changed for each of the three products. Names, prices, stock, images, previews, watch links, duration, product type and all other returned fields are unchanged.
- API contract: product GET fields differ from PATCH fields. The first product-32 request using GET names was a no-op except editTime; stopped and consulted current official API documentation. Correct documented PATCH fields are `summaryDescription` and `description`. All three final writes used CLI dry-run tokens, then exact readback. No safety limits bypassed.
- Source commit: 63c3eb6. New immutable asset `imweb-video-discovery-20260906.js`; predecessor remains unchanged. Scoped fallback only affects the three matching ID/code pairs, not unrelated products or explicit instructor names.
- Deployment: Firebase project `archive-pilates`, Hosting site `archive-pilates-home`, service account `archive-codex-operator@archive-pilates.iam.gserviceaccount.com`. One new asset uploaded. All seven configured predeploy checks and postdeploy classroom canary passed.
- Imweb save: `home@archivepilates.com`, SEO Header Code only, one discovery filename replaced. Reloaded editor matches prepared 26831-character value exactly. No other header text or unit/body/footer code changed.
- Live verification: exact asset/source match, new loader appears once and old loader is absent. Widths 320/390/768/1440 show `전체`, `민진쌤`, `은영쌤`, all three confirmed products among 20 Minjin results, no horizontal overflow. All three public detail pages show the corrected instructor in summary and detail.
- Ordinary-member regression: mobile/desktop buyer six expected cards and nonbuyer zero; 24/24 access checks pass. Both pinned test-account group snapshots restored and independently verified. The known Imweb header-more-menu warning appeared twice but did not affect these checks. Actual payments/refunds and playback were not performed.
- Classroom asset remains SHA-256 `7ef12cf0c0d2122597a6bd39dde4d97ac79c882c926964771e395c676fa0e328`, asset 2026-09-04c and loader 2026-09-04d. No auth/entitlement/Functions changes. No new CORE operating rule needed for this metadata-only correction.
- Cleanup: task-owned docs/admin/browser tabs and Playwright contexts closed, regression lock released. Pre-existing untracked live-verification HTML preserved outside this commit.
